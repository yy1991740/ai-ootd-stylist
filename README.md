# AI OOTD Stylist (AI 穿搭点评师)

一个由 AI 驱动的时尚顾问 Web 应用。它可以分析您的每日穿搭（OOTD），提供专业的评分、风格识别、配色提取，并给出个性化的改进建议。

> **由火山引擎豆包大模型驱动 (Doubao-1.5-Pro & Doubao-1.6-Vision)**

## ✨ 功能特性

- **AI 智能穿搭分析**:
  - **智能评分**: 基于风格、协调性和美感生成评分（1-100分）。
  - **风格识别**: 自动识别穿搭风格（如：法式慵懒、美式复古、简约通勤等）。
  - **配色提取**: 提取服装主色调，并显示精确的 HEX 色值和颜色名称。
  - **个性化反馈**: 提供穿搭亮点分析和具体的改进建议。
  - **场合推荐**: 推荐该穿搭适合的社交或工作场合。
- **多模型支持**: 支持在 `Doubao 1.5 Pro` 和 `Doubao 1.6 Vision` 模型之间自由切换。
- **多语言支持**: 完美支持简体中文、英语和印尼语。
- **现代 UI/UX 设计**:
  - 响应式设计，适配移动端和桌面端。
  - 支持拖拽上传图片，并提供实时预览。
  - 精美的数据可视化结果卡片。
  - 基于 Tailwind CSS 和 Lucide Icons 构建。

## 🛠 技术栈

- **前端框架**: React 18+ (Vite)
- **开发语言**: TypeScript
- **样式库**: Tailwind CSS
- **图标库**: Lucide React
- **AI 服务**: 火山引擎 Ark API (Doubao Vision Models)
- **部署平台**: Vercel (推荐)

## 🚀 快速开始

### 前置要求

- Node.js (v16 或更高版本)
- npm 或 yarn
- 有效的 [火山引擎 (Volcengine Ark)](https://www.volcengine.com/product/ark) API Key

### 安装步骤

1. **克隆项目**
   ```bash
   git clone https://github.com/yourusername/ai-ootd-stylist.git
   cd ai-ootd-stylist
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **配置环境变量**
   在项目根目录下创建一个 `.env` 文件：
   ```bash
   touch .env
   ```
   添加您的 API Key：
   ```env
   API_KEY=your_volcengine_api_key_here
   ```
   *(注意：Key 通常以 `sk-` 开头)*

4. **启动开发服务器**
   ```bash
   npm run dev
   ```
   在浏览器中打开 `http://localhost:5173`。

## 📦 构建与生产

生成生产环境的静态文件：

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

## ☁️ 部署指南 (Vercel)

本项目针对 Vercel 部署进行了优化。

1. 将代码推送到 GitHub 仓库。
2. 在 Vercel 中导入该项目。
3. **关键步骤**: 在 Vercel 项目设置中，进入 **Settings > Environment Variables**。
   - Key: `API_KEY`
   - Value: `您的_sk_..._密钥`
4. 点击 **Deploy** 进行部署。

## 📁 项目结构

```
├── components/        # React UI 组件
│   ├── Header.tsx     # 顶部导航与控制器
│   ├── UploadZone.tsx # 图片上传区域
│   └── ResultCard.tsx # 分析结果展示卡片
├── services/
│   └── doubaoService.ts # 火山引擎 API 集成服务
├── utils/
│   └── translations.ts  # 国际化文案与 AI 提示词 (Prompt)
├── types.ts           # TypeScript 类型定义
├── App.tsx            # 主应用逻辑
└── vite.config.ts     # Vite 配置 (处理环境变量注入)
```

## 📝 配置详情

### 模型配置
应用当前支持以下模型端点（定义在 `types.ts` 中）：
- `doubao-1.5-vision-pro-250328` (默认)
- `doubao-seed-1-6-vision-250815`

### 提示词工程 (Prompt Engineering)
提示词位于 `utils/translations.ts` 中。我们对提示词进行了专门调优，以克服 AI 模型通常较为保守的评分倾向，鼓励对优秀的穿搭给出 90 分以上的高分，从而提供更具鼓励性的用户体验。

## 📄 许可证

MIT License